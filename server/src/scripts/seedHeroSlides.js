// Backward-compatible entry point for the original seed command.
// The canonical Prisma seed creates missing launch slides and never overwrites
// content managed through the Admin Hero editor.
import "../../prisma/seedHeroSlides.js";
